# Electro House v2

This data-only style composes reusable rhythmic patterns with generic `pattern`,
`chord_pattern`, and `motif` generators. Songs select variations and fills by
name; the compiler contains no knowledge of this style ID.
